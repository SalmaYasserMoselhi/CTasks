#ifndef EDITOR_H
#define EDITOR_H

int run_editor_new();      // start new editor session (returns 0 normally)
int run_editor_display();  // display saved buffer (returns 0 normally)

#endif // EDITOR_H